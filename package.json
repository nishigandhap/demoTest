{
  "name": "saucedemo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "cypress run cypress/e2e/TestPage/*.js --headed --env allure=true",
    "allure:report": "allure generate allure-results --clean -o allure-report",
    "reportTest": "npm run test && npm run allure:report"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.3.0"
  }
}
