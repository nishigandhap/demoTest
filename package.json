{
  "name": "saucedemo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "cypress run cypress/e2e/TestPage/*.js --headed --env allure=true",
    "allure:report": "allure generate allure-results --clean -o allure-report",
    "reportTest": "cypress run cypress/e2e/TestPage/*.js --headed --env allure=true && allure generate allure-results --clean -o allure-report"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.3.0"
  }
}
